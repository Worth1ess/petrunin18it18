package testing;
import org.junit.Assert;
public class MainTest {
@org.junit.jupiter.api.Test
void main() {
Int a2=5; int a5=60; int b2=100; int b5=8; int a=9; 
int b=90;
int m=800;	
 int test = TestingMain.test(a3);
 int actual = 5;
 Assert.assertEquals(test, actual); } }




package testing;
import java.io.FileInputStream; import java.util.Scanner; import java.util.logging.Level; import java.util.logging.LogManager; import java.util.logging.Logger;
class TestingMain { public static Scanner scan = new Scanner(System.in); private static Logger logger;
static {
    try (FileInputStream configFile = new FileInputStream("C:\\Users\\w1s\\IdeaProjects\\Software Design nstruments\\src\\logging\\loggerConfigurations\\logger.config")){
        LogManager.getLogManager().readConfiguration(configFile);
        logger = Logger.getLogger(TestingMain.class.getName());
    } catch (Exception ignore) {
        ignore.printStackTrace();
    }
}

public static void main(String[] args) {
    logger.log(Level.INFO, "Начало программы, присваивание значении для переменных");

 int a2;  a5;  b2;  b5;  a; b;  m; 
logger.log(Level.INFO, "Присваивание значении для переменных"); 
Int a2=5; a5=60; b2=100;  b5=8;  a=9; b=90;  m=800;
test = TestingMain.test(a3);
        System.out.println(test);
        logger.log(Level.INFO, "Конец программы");
    }


    public static int TestingMain(Int a2, int a5 , int b2, int  b5 , int a=9 , int b , int  m) {
        for (i = a2; i <= a5; i++) {
            logger.log(Level.INFO, "Проверка условия { TestingMain (i) < test} ");
            if (TestingMain(i) < R) {
                logger.log(Level.INFO, "Условие { TestingMain(i) < test} верно ");
              
                test = TestingMain.test(a2); 
            }
        }

    }
}
